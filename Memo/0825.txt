0825

JS배열
ㄴ for-of문 : for (let idx of week){} : for-each문과 비슷
ㄴ for-in문 : ?

특정상황을 거르기 위해서 boolean flag라는 스위치를 만들어서 사용하는 상황이 많다! 기본값은 boolean이니까 false값이고 특정상황에 flag = true값을 주어 걸리게 만든다.
그리고 마지막 출력에서 flag = true값인 상황을 거르는 if문 같은 걸 만듬

일단 완성!해라

배열
ㄴ 배열과 객체는  출력했을때 결과가 같다
ㄴ 둘다 주소를 출력함


자바에서 배열 
ㄴ /* arrayinsert
 - 자바에서는 문자열 동등 비교 시 '==' 연산자를
사용하면 제대로 비교가 되지 않습니다.
- 문자열 String 타입이 기본 데이터 타입이 아닌
객체 참조 타입이기 때문입니다.
객체 참조 타입은 해당 값을 직접 들고 있는 게 아니라
객체의 주소값을 들고 있기 때문에,
문자열이 아무리 같더라도 주소가 다르면 false를
도출합니다.
- 그러므로 문자열 동등 비교를 진행할 때는
[비교문자열1.equals(비교문자열2)] 를 사용하여
진행해야 합니다.
*/
ㄴ System.out.println(Arrays.toString(names)); 얘는 자바에서 제공하는 배열출력
ㄴ ArrayInsert코드 복붙해서 설명적기
ㄴ 배열의 수정 : 기존의 값을 밀어내고 새로운 값을 집어넣는다.
ㄴ foods[0] = "치킨"; 이러케
ㄴ 수정이나 insert나 비슷하다고 볼 수 있다.
ㄴ 자바의 배열은 크기가 늘거나 줄 수 없기 때문에 '삭제'라는 개념은 없다.
ㄴ 그대신 '삭제'처럼 보이는 개념은 있다.
ㄴ 











Q. int max mid min 이거 초기화 0으로 미리 하면 안좋은건지
A. 아님 에러났을 때 값이 0으로나와서 좀 더 수정하기 쉽대요

Q. 그럼 null 보기싫으면 그냥 수정하면되는거아닌

