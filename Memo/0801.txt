0801
~~~~JAVASCRIPT 진짜 시작!~~~~
1. 변수 선언 후, 초기화
2. 함수(function) : 호출해야 수행이 되는 애들
-함수는 호출을 해야 수행이된다!
3. 자바스크립트는 자료형이 없다.(내부적으로는 존재하며, 값이 할당될 때 자동으로 지정된다.) ->지가하겟대 정하지말래
4. 자바스크립트는 ''과 ""의 구분을 하지않음
- 변수나 함수명 들은 절대 숫자로 시작하면 안된다~
- 함수 호출시, 함수명 그대로 적어준다.
- 함수 밖에 변수 선언한건, 남아있으면서 만들어지지만
  함수 안에서 선언된 변수는 함수 호출 후에 삭제된다.(=남아있지X)
- 함수 안에서 선언된 변수 : 지역변수
- 문자열 + 숫자 -> 문자열 + 문자열 로 바꿔버림
그래서 "a + b : "+a+b 는  걍 a + b : 1030 이되버렸다!
숫자로 나오게 하고싶다 -> ()치기
서버로 데이터 보낼때 쓰는 태그!!!!!!!!!! form!!!!!!!!!!!!!!
sid.focus(); 아이디가 s_id인 요소에게 커서를 넣어준다
비교연산자는 좌측을 기준으로 연산해서 true false 값이 나온다.
JS에서 비교연산자를 쓰면 출력값이 boolean형으로 나온다

innerText : 넣어줄 값이 그냥 텍스트만 있으면 사용/ 어느칸 어느 공간안에 텍스트를 출력할경우
innerText는 변수에다가 넣을 수 없다
- box.innerText = "a += val1 : " + a;
ㄴ box id를 가진 오픈,크로즈 태그 안에 우항의 값을 넣어주겠다.
innerHTML : 넣어줄 문자열 중에 html코드가 있어서 html로 해석이 필요한 내용이라면 사용 / ""안에 css요소를 넣어도 그게 적용되어서 출력가능
대입연산자가 우선순위가 제일 낮음
 증감연산자 젤루 중요
증감연산자는 ++, -- 2개뿐! 
period연산자 : 특정한 개체를 접근할 때 쓰는 연산자
ㄴ document.write
변수 앞에 증감연산자가 있으면 우선순위가 가!장! 높음(++a)
근데 변수 뒤에 증감연산자가 있으면 진짜진짜제일 뒷순위임(a++)
&& : c = (++a < b-- && ++a != 0); //첫번째 조건이 true여야 뒷조건도 수행이 가능하다.
|| : c = (++a < b-- || ++a != 0); //얘는 첫번째 조건이 true가 나오면 뒷조건을 수행하지 않는다.
//제어문
            //  1. 비교문 : if문, switch문
            //  2. 반목문 : for문, while문, do-while문
            
            // if문의 구성(조건식은 반드시 결과가 true아니면 false여야 한다.)
            
            //  if(조건식)
            //      조건식에 만족시 수행하는 문장;


            //  if(조건식) {
            //      조건식에 만족시 수행하는 문장1;
            //      조건식에 만족시 수행하는 문장2;
            //      조건식에 만족시 수행하는 문장3;
            //  }
